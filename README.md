# RRHH Frontend PRO v4 (visual upgrade)
Mejoras visuales sin tocar lógica ni endpoints: tema refinado SAP/Atlassian, AppBar translúcida, sidebar moderna, tarjetas glass, DataGrid toolbar pro, login con loader.

## Run
```bash
cp .env.example .env   # VITE_API_URL=http://localhost:3000
npm install
npm run dev
```